#include<iostream>
#include<cmath>
using namespace std;

int main(){

int time = 20;
string result = (time<18) ? "good day ." : "good evening." ;
//ternary operator is used instead of if to make it one line code

    return 0;
}